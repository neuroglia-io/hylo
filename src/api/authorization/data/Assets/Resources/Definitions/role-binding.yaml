apiVersion: core.hylo.cloud/v1
kind: ResourceDefinition
metadata:
  name: role-bindings.authorization.hylo.cloud
spec:
  names:
    kind: RoleBinding
    singular: role-binding
    plural: role-bindings
    shortNames:
      - rb
  group: authorization.hylo.cloud
  version: v1
  scope: cluster
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        type: object
        properties:
          spec:
            type: object
            properties:
              role:
                $ref: https://core.hylo.cloud/v1/schemas/resource-reference.json
              subjects:
                type: array
                items:
                  $ref: https://core.hylo.cloud/v1/schemas/resource-reference.json
            required:
              - role
              - subjects
        required:
         - spec
apiVersion: admission.hylo.cloud/v1
kind: ResourceDefinition
metadata:
  name: validating-webhook
spec:
  names:
    kind: ValidatingWebhook
    singular: validating-webhook
    plural: validating-webhooks
    shortNames:
      - vwh
  group: admission.hylo.cloud
  version: v1
  scope: global
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        type: object
        properties:
          spec:
            type: object
            properties:
              client:
                type: object
                properties:
                  uri:
                    type: string
                    format: url
                required:
                  - uri
              resources:
                type: array
                items:
                  type: object
                  properties:
                    scope:
                      type: string
                    apiGroups:
                      type: array
                      items:
                        type: string
                    apiVersions:
                      type: array
                      items:
                        type: string
                    kinds:
                      type: array
                      items:
                        type: string
                    operations:
                      type: array
                      items:
                        type: string
              priority:
                type: number
            required:
              - client
        required:
          - spec